-- Ce script SQL affiche toutes les lignes et colonnes de la table 'first_table'
-- La commande SELECT * récupère toutes les colonnes (représentées par l'astérisque *)
-- FROM first_table indique la table source des données
-- Cette requête permet de visualiser l'ensemble du contenu stocké dans la table
SELECT * FROM first_table;
